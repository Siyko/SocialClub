<p id="notice"><%= notice %></p>
<h3>Tasks list</h3>

<% @tasks.each do |t| %>
    <p>
     <strong>
       <%= link_to t.title, task_path(t) %>
     </strong>
    </p>
<% end %>

<%= link_to 'New task', new_task_path %>